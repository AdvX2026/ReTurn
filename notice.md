# ReTurn 演示冲刺 Notice

> 适用范围：2026-07-27 演示版本及其之前的前端冲刺。
>
> 这是项目级的临时演示约束。发生冲突时，以用户最新明确要求为最高优先级；本文件覆盖各 App/package notice 中与生产完善度、旧系统兼容和开发节奏有关的相反约定。PRD 的产品语义、共享合同、数据完整性和安全边界不因演示而放宽。

## 当前目标

- 当前版本专门服务于明天的演示，不面向最终用户；所有工作先判断是否能直接改善演示结果。
- Windows 已正式纳入 v0.7 的下一阶段完整客户端范围，但不进入当前演示验收；当前演示仍只覆盖 Apple 客户端。
- 本轮以前端快速开发和修 bug 为主，优先优化 iOS 手机视图，并尽量保证 macOS 演示路径可用。
- 目标是在约 3 小时内完成主要前端工作。采用能解决当前问题的最小改动，不增加演示用不到的功能、抽象、重构或配置。
- 用户会持续给出具体意见。若某项建议对演示没有可见帮助、会挤占更重要的演示工作，或会损害安全、正确性与运行效率，应直接说明并反驳或建议延期，而不是机械执行。

## 演示路径优先

- 只实现和修复演示中实际会经过的页面、状态和交互。
- 优先级依次为：演示阻塞（编译、启动、崩溃）→ 演示路径中的数据/状态错误 → 关键交互错误 → 用户明确指定的界面调整。
- 不为演示路径之外的边缘场景、生产级完整性或未来扩展提前工作。
- `docs/context/context-6.md` 是当前 iOS Before/Main 前端约定的直接上下文；已合并的 Before 顶部遮罩、滚动显隐、迟滞和 Composer 状态保留属于现行基线，除非具体 bug 要求，否则不要重做或顺手改写。

## 平台与 API 策略

- 唯一验收环境是 iOS 26 和 macOS 26；当前冲刺不投入时间适配更低系统版本。
- 新实现优先使用 iOS 26/macOS 26 的最新原生 SwiftUI API 和系统 UI。禁止为了兼容旧系统而选择较旧实现。
- 不为新的演示代码增加低版本 fallback、旧 API 替代方案或手绘的系统效果仿制。
- iOS 是当前界面优化的主要平台；macOS 保持其独立产品布局，不强行复用 iOS Timeline。共享代码只承载真正共用的状态、数据或已有共享组件。
- 本文件本身不修改 deployment target。若具体实现必须调整 target 或删除既有兼容分支，应作为该任务内的明确代码/工程变更处理，并优先满足 iOS 26/macOS 26 演示，而不是新增旧版本方案。

## 文档与代码定位

- 快速开发不等于跳过文档。修改前先查与当前问题直接相关的 Apple/SwiftUI 文档，确认最新 API 的正确用法，以减少返工。
- 仓库存在 `.codegraph/`；理解或定位代码时先使用 CodeGraph，再按需要读取具体文件。
- 文档检索只围绕当前 bug，避免无边界研究。

## 验证节奏

- 不在每个小修改后频繁运行构建或全套检查；相关修改成批完成后再集中验证。
- 默认在本轮结束前最多进行一次相关验证。若只是风险很低的小改动，可以不运行检查，但交接时必须明确说明未验证及剩余风险。
- 若运行 Xcode 构建，应使用 clean build；本项目的增量构建曾出现 stale dylib，不能作为可靠的最终验证。
- 纯状态逻辑若容易覆盖，可增加最小单元测试并在最后统一运行；不为视觉调整临时建设截图或 UI 测试体系。

## 视觉工作边界

- Agent 不进行视觉检索、竞品/素材搜索、截图对比、视觉方案探索或需要用户观看的视觉呈现。
- 用户负责视觉判断与手工视觉验收。Agent 根据用户给出的具体表现和预期修改核心代码，重点保证状态、交互、数据流和 Swift 语法/编译结构正确。
- 视觉要求不明确时不自行发明方案；先指出缺少的决定，或在不阻塞其他工作的情况下继续处理确定部分。

## 不变边界

- 演示优先不授权放宽 secrets、凭据、隐私、LAN 安全边界、`client_uuid` 幂等性或 Pi SQLite 单一数据权威。
- 不因赶进度擅自修改 `packages/shared` 合同、Swift 合同镜像、数据 schema 或跨包架构。
- 演示结束后，应重新评估本文件中的临时覆盖规则，再决定恢复低版本兼容和生产级完善工作的范围。
